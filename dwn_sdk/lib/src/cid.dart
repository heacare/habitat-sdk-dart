/// CID parsing and generation.
class CID {
  /// Construct a new placeholder CID.
  CID();

  /// Construct a new placeholder CID from JSON.
  factory CID.fromJson() => CID();

  /// Serialize the CID to JSON.
  String toJson() => '';
}
